
module.exports = {
    name: 'flip',
    description: 'Flips a coin',
    usage: '',
    guildOnly: false,
    args: false,
    async execute(message,args) {
        let random = Math.floor(Math.random());
        message.reply(``)
    }
}